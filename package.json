{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "prod": "turbo run prod",
    "build:dev": "turbo run build:dev --filter=blog-client --filter=blog-admin && turbo run build --filter=blog-service",
    "build:prod": "turbo run build:prod --filter=blog-client --filter=blog-admin && turbo run build --filter=blog-service",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "turbo": "^2.9.11"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt"
    ]
  },
  "workspaces": {
    "nohoist": [
      "**/@mdx-js/react",
      "**/@mdx-js/react/**/*",
      "**/react"
    ]
  }
}
